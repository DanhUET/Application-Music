package pro.branium.infrastructure.repository.song

import pro.branium.feature_song.domain.repository.ReportRepository
import javax.inject.Inject

class ReportErrorRepositoryImpl @Inject constructor() : ReportRepository {
    override fun report(songId: String) {
        // todo
    }
}