package pro.branium.infrastructure.navigator

import pro.branium.core_navigation.navigator.ArtistNavigator
import javax.inject.Inject

class ArtistNavigatorImpl @Inject constructor() : ArtistNavigator {
    override fun openArtistDetail(artistId: Int) {
        // todo
    }
}