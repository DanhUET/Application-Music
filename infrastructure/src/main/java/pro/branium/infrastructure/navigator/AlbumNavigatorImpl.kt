package pro.branium.infrastructure.navigator

import pro.branium.core_navigation.navigator.AlbumNavigator
import javax.inject.Inject

class AlbumNavigatorImpl @Inject constructor() : AlbumNavigator {
    override fun openAlbumDetail(albumId: Int) {
        // todo
    }
}